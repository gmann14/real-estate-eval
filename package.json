{
  "name": "real-estate-eval",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "node scripts/lint-package.mjs",
    "test": "node --test",
    "test:vitest": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "yaml": "latest"
  },
  "devDependencies": {
    "@types/node": "latest",
    "ts-node": "latest",
    "typescript": "latest",
    "vitest": "latest"
  }
}
